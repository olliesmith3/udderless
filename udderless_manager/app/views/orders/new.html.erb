<%= javascript_pack_tag 'order_form' %>

<div>
  <button id="ho">ho</button>
</div>
<div id="hey">
  <h1>heyyyy</h1>
</div>

<h1 class="title">New Order</h1>
<h1 class="subtitle">Type of order</h1>
<div class="control">
  <button id="one-off">
    One Off
  </button>
  <button id="continuous">
    Continuous
  </button>
</div>

<div id="one-off-form">
  <%= form_with(model: [@customer, @order]) do |form| %>
    <div class="field">
      <%= form.label "Quantity", class: "label" %>
      <div class="control">
        <%= form.text_field :quantity, class: "input"%>
      </div>
    </div>
    <%= form.submit "Create", class: "button is-primary"  %>
  <% end %>
</div>
<div id="continuous-form">
  <%= form_with(model: [@customer, @order]) do |form| %>
    <div class="field">
      <%= form.label "Quantity", class: "label" %>
      <div class="control">
        <%= form.text_field :quantity, class: "input"%>
      </div>
    </div>
    <%= form.submit "Create", class: "button is-primary"  %>
  <% end %>
</div>
